[package]
name = "chainmail-linera"
version = "0.1.0"
edition = "2021"

[dependencies]
linera-sdk = "0.10"  # Use the latest version compatible with Linera
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"  # For error handling